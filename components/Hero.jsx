export default function Hero({ title, description, imageUrl }) {
    return (
        <div className="hero">
           
            <h1>{title}</h1>
        <p>{description}</p>
        </div>
    );
}
